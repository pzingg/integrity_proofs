defmodule DidServerWeb.PresentationController do
  use DidServerWeb, :controller

  def prove(conn, params) do
    json(conn, "TODO")
  end

  def verify(conn, params) do
    json(conn, "TODO")
  end
end
