defmodule DidServerWeb.PresentationController do
  use DidServerWeb, :controller

  def prove(conn, _params) do
    json(conn, "TODO")
  end

  def verify(conn, _params) do
    json(conn, "TODO")
  end
end
